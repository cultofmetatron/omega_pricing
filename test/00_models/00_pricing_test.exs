defmodule PriceTracker.PriceTest do
  use ExUnit.Case
  #doctest PriceTracker
  @valid_scenario_1 %{
  
  }

  @invalid_scenario_1 %{
  
  }

  describe "Price model" do
    test "it must have a name, price and external id"
  end
end
